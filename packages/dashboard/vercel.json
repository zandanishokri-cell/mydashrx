{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "installCommand": "cd ../.. && npm install && cd packages/shared && npm run build && cd ../dashboard && mkdir -p node_modules/@mydash-rx && ln -sfn ../../../shared node_modules/@mydash-rx/shared",
  "outputDirectory": ".next",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options", "value": "DENY" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" }
      ]
    }
  ]
}
